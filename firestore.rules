
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Completely open for the next 30 days for demo purposes.
      allow read, write: if request.time < timestamp.date(2024, 8, 14);
    }
  }
}
