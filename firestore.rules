rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // THIS IS A TEST RULE - It allows anyone to read and write to any document.
    // This is NOT secure for a production app.
    // TODO: Secure your database before launch.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
