rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules are completely open and are for temporary
    // development and testing ONLY. They provide no security.
    // You MUST secure your database before going to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
