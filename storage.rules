rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // THIS IS A TEST RULE - It allows anyone to read and write any file.
    // This is NOT secure for a production app.
    // TODO: Secure your storage before launch.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
