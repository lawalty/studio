rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read access to anyone
    match /{allPaths=**} {
      allow read;
      allow write; // Allow writes to enable file uploads from the admin panel
    }
  }
}
