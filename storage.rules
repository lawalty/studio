rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read and write access for the next 30 days.
      // This is a temporary rule for development and demonstration.
      allow read, write: if request.time < timestamp.date(2024, 8, 9);
    }
  }
}
