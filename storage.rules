rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read and write access to all files.
    // This is suitable for a demo application without user authentication.
    // For a production app, you would implement more secure rules.
    match /{allPaths=**} {
      allow read, write;
    }
  }
}
