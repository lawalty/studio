rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // TEMPORARY TEST: Allow all reads and writes to diagnose the upload issue.
    // This is insecure and should be reverted after the test.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
