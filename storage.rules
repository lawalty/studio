rules_version = '2';

// WARNING: These rules are for temporary development and are not secure.
// They allow public read and write access to all paths.
// You must secure these rules before deploying to production.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write;
    }
  }
}
