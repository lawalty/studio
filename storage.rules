rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read, write for all paths if the user is authenticated.
    // This is secure for this application because all write operations
    // originate from the protected admin panel.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
