rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allows anyone to read files (for public downloads and chatbot display).
      allow read;

      // Only allows authenticated entities to write files (for updating your RAG knowledge base).
      allow write: if request.auth != null;
    }
  }
}
