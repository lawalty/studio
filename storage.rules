rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // WARNING: TEMPORARY DEBUGGING RULE
    // This allows anyone, authenticated or not, to read and write to storage.
    // This is insecure and MUST be changed back.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
